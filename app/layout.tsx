import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Navigation } from "@/components/navigation"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "CITED - Center for Innovation, Technology & Economic Development",
  description:
    "Innovating Today, Transforming Tomorrow - Global center of excellence for innovation, technology advancement, and sustainable economic development",
  keywords:
    "innovation, technology, economic development, education, certification, fellowship, digital transformation",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
